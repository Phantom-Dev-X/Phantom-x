# peer dep, but our project uses sharp@^0.34. Optional peer mismatches are
# safe to ignore — this flag tells npm to do that automatically on every
# install, on every host (Render, Replit, Railway, VPS, local). Without
# this file, Render's npm install fails with ERESOLVE.
legacy-peer-deps=true
